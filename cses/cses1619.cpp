#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m, a;
    cin >> n >> m;
    set<int> b; 
    for (int i = 0; i < n; i++)
    {
        cin >> a;
        b.insert(a);
    }
    sort(b.rbegin(), b.rend());

    for (int i = 0; i < m; i++)
    {
        cin >> a;
        for(auto it: b){
            if(b)
        }
        
    }
    
    
}